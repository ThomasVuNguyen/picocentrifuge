---
description: Add images to the piece of writing so that it's more interactive
---

Take images from the codebase or from onshape render (using the api keys) to add to the piece of writing. There should be an image (or set of images) every few sentences or 1-2 paragraphs.

Videos also count too.

Before deciding what images/videos to put where, analyze all images and video frames for context.